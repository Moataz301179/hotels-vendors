name: Deprecated Deploy Flow (Do Not Use)

on:
  workflow_dispatch:

jobs:
  deprecated:
    name: Deprecated deployment guard
    runs-on: ubuntu-latest
    steps:
      - name: Explain canonical deployment
        run: |
          echo "This workflow is deprecated."
          echo "Use .github/workflows/deploy.yml (PM2) as the only production deployment path."
