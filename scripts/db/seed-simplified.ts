/**
 * ⚠️ DEPRECATED — This seed script references removed models (Organization, FactoringPartner)
 * that no longer exist in the current schema.
 *
 * Use the canonical seed instead:
 *   npx prisma db seed          # runs prisma/seed.ts
 *   npm run db:seed             # runs tsx prisma/seed.ts
 *   npx tsx prisma/seed-extended.ts  # extended demo data
 *
 * This file is kept for reference only and will be removed in a future cleanup.
 */

console.error("⚠️  This seed script is DEPRECATED.");
console.error("Use: npx prisma db seed  (or  npm run db:seed)");
console.error("Extended: npx tsx prisma/seed-extended.ts");
process.exit(1);
